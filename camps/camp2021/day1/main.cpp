#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using ld = long double;

#define SZ(X) (int)((X).size())
#define ALL(x) (x).begin(), (x).end()

#define TMAX(type) n

int main() {
}
